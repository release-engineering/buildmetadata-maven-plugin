import java.io.*;

File file = new File(basedir, "target/bmd-it-war-1.0.0-SNAPSHOT/META-INF/build.properties");
if (!file.isFile())
{
	throw new FileNotFoundException("Could not find generated build.properties: " + file);
}